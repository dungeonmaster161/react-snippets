import React from 'react'

/*
UseEffect hook is use to perfom side effect in function components . 
Side effect are opration that can affect other component or cant be done during 
rendering , such as data fetching , subscriptions , or manually changing the dom in 
react components

The useEffect hook serves the same purpose as 'componentDidMount', 'componentDidUpdate' and
'componentWillMount' in react class components but inified into single api


useEffect(functon(){},[]) [] means it only runs once when components renders
[] takes state variable as input any time [count] this count changes this useEffect will be called
*/

export default function HookUseState() {
  return (
    <div>
      
    </div>
  )
}
